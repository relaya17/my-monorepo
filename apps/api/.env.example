## API environment (example)
## Copy to apps/api/.env and fill real values (do NOT commit .env)
## See docs/vantera/SECURITY.md for full list.
## Render.com: docs/vantera/RENDER_DEPLOY.md – JWT_SECRET חובה ב-production!

# Required
MONGO_URI=mongodb://localhost:27017/your_db
# Example Atlas format:
# MONGO_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.example.mongodb.net/your_db?retryWrites=true&w=majority

# Required in production (Render, etc.) – min 32 chars. Generate: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32

# Server
PORT=3008
NODE_ENV=development

# Logging (create apps/api/logs/ if using file)
LOG_LEVEL=info
LOG_PATH=logs/server.log
# Optional: RATE_LIMIT_WINDOW_MS=900000, RATE_LIMIT_MAX_REQUESTS=100, LOGIN_RATE_LIMIT_MAX=5

# Redis (optional)
REDIS_URL=redis://localhost:6379

# JWT (see JWT_SECRET above – required in production)
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS & URLs (production)
CORS_ORIGIN=https://app.hsll.com
LANDING_PAGE_ORIGIN=https://hsll.com
FRONTEND_URL=http://localhost:5173
# Must match vite.config.ts server.port (currently 5173)
CLIENT_URL=https://app.hsll.com

# Stripe (optional)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PLATFORM_FEE_BPS=
# CLIENT_URL used by Stripe redirects

# Cloudinary (uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Alerting (5xx) – set one or both
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# AI
AI_CRON_ENABLED=true
OPENAI_API_KEY=

# Email (Onboarding – Resend)
RESEND_API_KEY=
EMAIL_FROM=onboarding@vantera.co.il

# אדמין: בהפעלה ראשונה נוצר אדמין ברירת מחדל (אם אין אף אדמין ב-DB): username=admin, password=admin123
# בדיקה: GET /api/admin/check. התחברות: POST /api/admin/login
# Super-admin: set Admin.role to 'super-admin' in DB for CEO dashboard (GET /api/super-admin/activity-stream, /global-stats)

# Public API (no auth): GET /api/public/impact-metrics, /api/public-stats
